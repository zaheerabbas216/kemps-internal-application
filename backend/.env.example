PORT=8000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=kemps_inventory
JWT_SECRET=change_this_for_production

# Comma-separated list. Local and the Cloudflare frontend domain are allowed by default.
# CORS_ORIGINS=http://localhost:5173,https://kmps-fed-fullstack-application.bitwizard.online
